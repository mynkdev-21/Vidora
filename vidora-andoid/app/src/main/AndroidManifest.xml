<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vidora"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Fix AdServices config conflict between AdMob and Firebase -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- AdMob App ID (Test ID — update in Constants.java for production) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9387570944207452~6751708815" />

        <!-- Splash (Launcher) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home -->
        <activity android:name=".ui.home.HomeActivity" android:exported="false" />

        <!-- Auth -->
        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity android:name=".ui.auth.SignupActivity" android:exported="false" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" android:exported="false" />

        <!-- File Detail (handles deep links from share page) -->
        <activity
            android:name=".ui.filedetail.FileDetailActivity"
            android:exported="true">
            <!-- Custom scheme: vidora://view/TOKEN -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vidora" android:host="view" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vidora" android:host="download" />
            </intent-filter>
            <!-- HTTP deep link: http://IP:8080/v/TOKEN -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:pathPrefix="/v/" />
                <data android:scheme="https" android:pathPrefix="/v/" />
            </intent-filter>
        </activity>

        <!-- Player (opened from FileDetail) -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />

        <!-- Paste Link -->
        <activity android:name=".ui.paste.PasteLinkActivity" android:exported="false" />

        <!-- Search -->
        <activity android:name=".ui.search.SearchActivity" android:exported="false" />

        <!-- Feed (Subscriptions) -->
        <activity android:name=".ui.feed.FeedActivity" android:exported="false" />
        <activity android:name=".ui.feed.SubscriptionsListActivity" android:exported="false" />
        <activity android:name=".ui.creator.CreatorProfileActivity" android:exported="false" />
        <activity android:name=".ui.upload.UploadActivity" android:exported="false" />

        <!-- Saved/Bookmarks -->
        <activity android:name=".ui.saved.SavedActivity" android:exported="false" />

        <!-- Profile -->
        <activity android:name=".ui.profile.ProfileActivity" android:exported="false" />

        <!-- Privacy Policy -->
        <activity android:name=".ui.webview.PrivacyActivity" android:exported="false" />

        <!-- Terms & Conditions -->
        <activity android:name=".ui.webview.TermsActivity" android:exported="false" />

        <!-- Help & Support -->
        <activity android:name=".ui.webview.HelpActivity" android:exported="false" />

        <!-- About -->
        <activity android:name=".ui.about.AboutActivity" android:exported="false" />

        <!-- Contact Support -->
        <activity android:name=".ui.contact.ContactActivity" android:exported="false" />

        <!-- Image Viewer -->
        <activity android:name=".ui.imageviewer.ImageViewerActivity" android:exported="false" />

        <!-- Notifications -->
        <activity android:name=".ui.notifications.NotificationsActivity" android:exported="false" />

        <!-- Downloads -->
        <activity android:name=".ui.downloads.DownloadsActivity" android:exported="false" />

        <!-- Subscription -->
        <activity android:name=".ui.subscription.SubscriptionActivity" android:exported="false" />

        <!-- FileProvider for sharing downloaded files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- FCM Service -->
        <service
            android:name=".fcm.VidoraMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
